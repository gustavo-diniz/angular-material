PROJETO ANGULA + MATERIAL + Springboot

Exemplo de tela enviando um arquivo xml e e sendo processo pela api java em springboot.

Para compilar:

- Angular:

npm install
ng serve --open


- Java:

mvn clean install
java -jar targer/springBootApi.jar




